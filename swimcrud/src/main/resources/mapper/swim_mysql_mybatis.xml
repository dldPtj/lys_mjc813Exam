<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mjc813.swimcrud.swim.ISwimMybatis">

    <select id="findAll" resultType="SwimDto">
        SELECT
            id
            , name
            , lanes
            , lane_type
            , phone_number
            , addr
        FROM swimpool_tbl
        ORDER BY id
    </select>

    <insert id="addSwim" parameterType="SwimDto">
        INSERT INTO swimpool_tbl (
            name
            , lanes
            , lane_type
            , phone_number
            , addr
        ) VALUES (
            #{name}
            , #{lanes}
            , #{lane_type}
            , #{phone_number}
            , #{addr}
        )
    </insert>

</mapper>